import { Client, TextChannel } from "discord.js";
import { createLoungeQueue } from "../common/messageHelpers";

export async function openMktLoungeQueue(client: Client) {
    // const channel = await client.channels.fetch('TODO')
    // if (!(channel instanceof TextChannel))
    //     throw new Error(`openMktLoungeQueue.ts channel is not a TextChannel`)

    // await createLoungeQueue('816786965818245190', channel, 60)
}